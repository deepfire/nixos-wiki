Terms and Definitions
---------------------

If you come across a term or word you don't know, add it here.

  ---------------------------------------------------------------------------
  Term
  Context
  Meaning
  Related Links
  ------------------ ------------------ ------------------ ------------------
  `rec { }`          `expression evalua Derivation         `stdenv`
  Nix expressions    tor`               Nix Manual         Nix Manual
  The `{ }` block    Nix Manual         A Nix expression   An attribute which
  contains "mutually The part of the    which describes a  contains things
  recursive"         Nix program which  build action. When expected in the
  attributes, which  reads and          evaluated, it      most basic Unix
  means they can     evaluates a Nix    creates one or     environment. (e.g.
  refer to each      expression.        more entries in    Bash shell, `gcc`,
  other.             [Nix Manual:       the Nix Store. The `cp`, `tar`,
  [Composing the     Common             `nix-instantiate`  `grep`, etc.)
  Hello              Options](http://ni command is the     [all-packages.nix:
  Package](http://ni xos.org/nix/manual command which      `stdenv =`](https:
  xos.org/nix/manual /#sec-common-optio creates entries in //github.com/NixOS
  /#ex-hello-composi ns) `--arg`\       the Nix Store,     /nixpkgs/blob/mast
  tion)               [Nix Manual:      while the          er/pkgs/top-level/
                     Built-in           `nix-env` and      all-packages.nix#L
                     Functions](http:// `nix-build`        224)]
                     nixos.org/nix/manu commands are its   
                     al/#ssec-builtins) user-friendly      
                                        interface.         
                                        [Nix Manual:       
                                        Glossary -\>       
                                        Derivation](http:/ 
                                        /nixos.org/nix/man 
                                        ual/#gloss-derivat 
                                        ion)\              
                                                           
                                        [<http://nixos.org 
                                        /nix/manual/#ssec- 
                                        derivation>        
                                        Nix Manual:        
                                        Derivation         
  ---------------------------------------------------------------------------


