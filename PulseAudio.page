mplayer
=======

     $ cat <<EOF
     let
       pkgs = (import <nixpkgs>) {};
     in
       pkgs.MPlayer.override { pulseSupport = true; }
     EOF > ~/.nix-defexpr/mplayer.nix

     $ nix-env -iA mplayer
